# Axis - v0.1.0


## New:
- Add theme `Dark Pink`
- Add app build number to about page version
- Add icon to `Last 7 Days` foldable section in `Weight Monitor` page
- Add `Appearance` section to settings, with theme changer dropdown
- Add the base for the user's `Profile` page
- Add navigation button to the user's `Profile` page
- Add animation to the `Navigation Drawer` expanding/collapsing
- Random greeting to the `Dashboard` page



## Fixes:
- App goes over TaskBar when maximized
- Minor bug fixes



## Updates:
- Reverting back to standard versioning
- Replace emojis in `Todo` page foldable sections with icons
- Navigation buttons will always be visible even on smaller screens as small side buttons same as when the side bar is collapsed
- Increase the font size and line height in the expanded side bar for the version and copyright notes
- Improve the `Navigation Drawer` visuals
- Improve app confgurations loading
- Main app major performance improvement
- `Navigation Drawer` major performance improvement
- Images renderer major performance improvement
- Minor improvements



## Removed:
- `Personal` and `General` sections in settings
